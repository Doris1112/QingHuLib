<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/dp_80"
    android:background="@android:color/white"
    android:gravity="center_vertical"
    android:padding="@dimen/dp_10">

    <TextView
        android:id="@+id/lib_notification_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:singleLine="true"
        android:text="@string/lib_download_progress"
        android:textColor="@color/gray_3"
        android:textSize="@dimen/text_size_17" />

    <ProgressBar
        android:id="@+id/lib_notification_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_5"
        android:layout_below="@+id/lib_notification_title"
        android:layout_toLeftOf="@+id/lib_notification_progress_text"
        android:layout_marginTop="@dimen/dp_10"
        android:max="100"
        android:progressDrawable="@drawable/lib_progressbar" />

    <TextView
        android:id="@+id/lib_notification_progress_text"
        android:layout_width="@dimen/dp_70"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/lib_notification_title"
        android:textColor="@color/gray_3"
        android:textSize="@dimen/text_size_15" />

</RelativeLayout>